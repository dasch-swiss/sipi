#!/bin/sh
# Wrapper script for using "zig c++" as CMAKE_CXX_COMPILER.
#
# CMake requires CMAKE_CXX_COMPILER to be a single executable path.
# This wrapper solves the "two-word command" problem by delegating to "zig c++".
#
# CMake passes target/cross flags (including -target) to this wrapper via
# compiler and linker flags initialized in cmake/zig-toolchain.cmake.

exec zig c++ "$@"
