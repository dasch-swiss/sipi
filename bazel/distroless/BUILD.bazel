"""Anchors the `bookworm.yaml` + `bookworm.lock.json` pair the
`apt.install` extension consumes from `MODULE.bazel`.

The lockfile is regenerated by:

    bazel run @bookworm//:lock

after editing `bookworm.yaml`.
"""

exports_files([
    "bookworm.yaml",
    "bookworm.lock.json",
])
