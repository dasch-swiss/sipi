coverage:
  status:
    project:
      default:
        target: auto  # maintain current coverage level
        threshold: 1% # allow 1% drop without failing
    patch:
      default:
        target: 50%   # new code should have ≥50% coverage
        threshold: 5%  # allow some slack for hard-to-test paths
