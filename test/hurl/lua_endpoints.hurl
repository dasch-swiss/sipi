# Lua test functions endpoint
GET {{host}}/test_functions
HTTP 200

# Lua mediatype test endpoint
GET {{host}}/test_mediatype
HTTP 200

# Lua mimetype function endpoint
GET {{host}}/test_mimetype_func
HTTP 200

# Lua knora session cookie endpoint
GET {{host}}/test_knora_session_cookie
HTTP 200
