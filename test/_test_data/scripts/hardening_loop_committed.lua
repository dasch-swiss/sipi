-- Commits the response head (first body byte streams it), then loops forever:
-- the deadline kill must abort the stream — the client must never see a clean
-- EOF.
server.print("started")
while true do
end
