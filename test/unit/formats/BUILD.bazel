"""TIFF format-handler unit tests (pyramid-level selection)."""

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "formats_tests",
    srcs = glob(["*.cpp"]),
    deps = [
        "//src/formats:formats",
        "@googletest//:gtest_main",
    ],
)
