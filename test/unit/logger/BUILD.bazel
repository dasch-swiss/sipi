"""Logger unit tests."""

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "logger",
    srcs = glob(["*.cpp"]),
    deps = [
        "//src:sipi_lib",
        "@googletest//:gtest_main",
    ],
)
