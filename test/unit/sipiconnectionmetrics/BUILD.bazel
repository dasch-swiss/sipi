"""SipiConnectionMetricsAdapter unit tests — verifies the adapter
forwards shttps connection-lifecycle events to SipiMetrics.
"""

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "sipi_connection_metrics_adapter_test",
    srcs = glob(["*.cpp"]),
    deps = [
        "//src:sipi_lib",
        "@googletest//:gtest_main",
    ],
)
