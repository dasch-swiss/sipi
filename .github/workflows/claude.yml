name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  # Gate on author_association (DEV-6884): comment-triggered workflows always run from the
  # default branch with secrets available, and "Require approval for all external
  # contributors" covers only fork pull_request events — without this gate any GitHub
  # account able to comment could spend the org's API key. The check sits inside each
  # trigger arm because issue_comment payloads also carry issue.author_association, so a
  # single AND-ed check would pass a stranger's comment on any issue or PR a member opened.
  # A blocked trigger skips silently, like a non-matching comment.
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude') &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude') &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude') &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.review.author_association)) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')) &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_args: "--model claude-opus-4-6"
          track_progress: "true"
          include_fix_links: "false"
          additional_permissions: |
            actions: read
          prompt: |
            When asked to "review" a PR, perform a thorough code review following
            the project's REVIEW.md checklist. Read REVIEW.md from the repository
            root first, then review every changed file against each applicable section.

            Post your findings as inline PR review comments on the specific lines
            where issues are found. Use GitHub's review API to submit a single review
            with all comments (not individual comments).

            Classify each finding:
            - 🔴 Request changes: security issues, memory safety violations, heap-buffer-overflow risks, type mismatches at C library boundaries
            - 🟡 Comment: style violations, missing RAII wrappers, test gaps, documentation gaps
            - 🟢 Approve: if no issues found

            Pay special attention to the "C library boundary safety (FFI)" and
            "Multi-buffer dimension safety" sections — these catch the class of
            bugs that sanitizers find at runtime.

            When reviewing, also check CI status — if the sanitizer workflow failed,
            include its findings in your review.

            For general "@claude" mentions (not "review"), respond normally.
