#
# This file needs to be updated whenever a new fuzzing subfolder is added.
#

# Fuzzing handlers
# To run this fuzzer, run from inside the build directory '(cd fuzz/ && ./fuzz_target_handlers)'
add_subdirectory(handlers)
